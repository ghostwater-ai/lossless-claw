---
"@martian-engineering/lossless-claw": minor
---

Add `lcm-read messages <conversationId>` with cursor pagination, token-budget paging, role/tool filters, deterministic truncation, and human/JSON outputs including pagination metadata.
